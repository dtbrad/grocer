<p><%= t('.greeting', recipient: @resource.email, default: "Welcome to Grocer, #{@resource.name}!") %></p>

<p>
   Your password is: <%= @resource.password %>
</p>

<p>Confirm your account email through this link :
  <%= link_to t('.action', default: 'Confirm my account'), confirmation_url(@resource, confirmation_token: @token, locale: I18n.locale) %>
</p>
