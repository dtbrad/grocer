<nav class="navbar-default">

  <div class="navbar-header">
    <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
      <span class="sr-only">Toggle navigation</span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
    </button>
  </div>

  <div class="collapse navbar-collapse navbar-custom" id="bs-example-navbar-collapse-1">

    <ul class="nav navbar-nav">
      <% if current_user %>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Shopping Lists <span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><%= link_to "Make a Shopping List", listmaker_path %></li>
            <li><%= link_to "See Previous Lists", shopping_lists_path %></li>
          </ul>
        </li>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Purchase History<span class="caret"></span></a>
          <ul class="dropdown-menu">
            <% if current_user && current_user.email == "sampleuser@mail.com" %>
              <li><%= link_to "View Purchase History", baskets_path %></li>
              <li><%= link_to "Import Receipts", sample_user_message_path, {:remote => true, 'data-toggle' =>  "modal", 'data-target' => '#modal-window'} %></li>
              <li><%= link_to "Delete Purchase History", sample_user_message_path, {:remote => true, 'data-toggle' =>  "modal", 'data-target' => '#modal-window'} %></li>
            <% elsif current_user && current_user.email != "sampleuser@mail.com" %>
              <% if current_user.baskets.length > 0 %>
                <li><%= link_to "View Purchase History", baskets_path %></li>
              <% end %>
              <li><%= link_to "Import Receipts", import_path_url, data: { confirm: 'You will be directed to your gmail now.' } %></li>
              <% if current_user.baskets.length > 0 %>
                <li><%= link_to "Delete Purchase History", disassociate_user_path(current_user.baskets.all), method: "post", data: { confirm: 'Are you certain you want to delete your history?' } %></li>
              <% end %>
            <% end %>
          <% end %>
            </ul>
          </li>
        <% if current_user && current_user.baskets.length > 0 %>
          <li><%= link_to "Products", products_path %></li>
        <% end %>
    </ul>

    <ul class="nav navbar-nav navbar-right">
      <% if current_user && current_user.admin? %>
        <li><%= link_to "Admin Page", nick_name_requests_path %></li>
      <% end %>
      <% if current_user %>
        <li>
          <%= link_to "How It Works", about_path %>
        </li>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" >Signed in as: <%= current_user.name %><span class="caret"></span></a>
          <ul class="dropdown-menu">
            <% if current_user.email != "sampleuser@mail.com" %>
            <li><%= link_to "Sign out", destroy_user_session_path, method: "delete"  %></li>
            <% else %>
            <li>
              <%= link_to "Register a real account!", log_out_to_register_path %></li>
              <% end %>
          </ul>
        </li>
      <% end %>
    </ul>

  </div>
</nav>
